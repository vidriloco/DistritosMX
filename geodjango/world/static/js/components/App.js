// Main App component with routing
function App() {
    // Use the simple router instead of React Router DOM
    return <SimpleRouter />;
}

// Make the component available globally
window.App = App; 